//
//  NFBDiagnosticsViewController.h
//
//  The debugger's face, on the device itself — nothing to plug in, no Console.
//  Shows the environment, the hook health, the decision log and the last
//  capture; shares the whole thing as a file from the sheet.
//

#import <UIKit/UIKit.h>

@interface NFBDiagnosticsViewController : UIViewController
@end
